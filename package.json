{
  "name": "lyricfren",
  "private": true,
  "packageManager": "yarn@4.9.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace frontend dev\" \"yarn workspace server start\"",
    "build": "yarn workspaces foreach --topological run build",
    "clean": "yarn workspaces foreach run clean"
  }
}
